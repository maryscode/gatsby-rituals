import { createGlobalStyle } from 'styled-components'

const ThemeFull = createGlobalStyle`
  #wrapper {
    margin: auto;
    max-width: 100%;
    font-family: sans-serif;
  }
`;

export default ThemeFull